% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_art_effort.R
\name{process_art_effort}
\alias{process_art_effort}
\title{Process Artisanal Fishing Effort Data by Grid Cell}
\usage{
process_art_effort(
  data,
  lon_col = "N_LONGITUD",
  lat_col = "N_LATITUD",
  sa_col = "SA",
  cell_size = 0.02
)
}
\arguments{
\item{data}{A data frame containing artisanal fishing effort data.}

\item{lon_col}{Character. The name of the longitude column in \code{data} (default is \code{"N_LONGITUD"}).}

\item{lat_col}{Character. The name of the latitude column in \code{data} (default is \code{"N_LATITUD"}).}

\item{sa_col}{Character. The name of the swept area (SA) column in \code{data} (default is \code{"SA"}).}

\item{cell_size}{Numeric. The size of each cell in degrees for the grid (default is 0.02).}
}
\value{
A spatial data frame (\code{sf}) with grid cells, average SA values, cell area, and SAR levels.
}
\description{
This function processes artisanal fishing effort data by grid cell, including filtering data,
creating a spatial grid, calculating the average swept area (SA) per grid cell, and determining
each cell's area.
}
\examples{
# Crear un conjunto de datos de ejemplo
data <- data.frame(
  N_LONGITUD = c(-71.3, -71.5),
  N_LATITUD = c(-33.5, -33.6),
  SA = c(0.1, 0.3)
)

# Aplicar la función
processed_data <- process_art_effort(
  data, lon_col = "N_LONGITUD", lat_col = "N_LATITUD", sa_col = "SA", cell_size = 0.02
)
}
